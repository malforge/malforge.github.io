**Assembly:** VRage.dll

```csharp
public enum MyMultipleEnabledEnum
```

Enumeration describing Enabled state of multiple objects.

## Fields

|Member|Description|
|---|---|
|AllDisabled|All blocks has off state|
|AllEnabled|All blocks has on state|
|Mixed|Blocks has on and off state|
|NoObjects|No blocks with on/off state|

