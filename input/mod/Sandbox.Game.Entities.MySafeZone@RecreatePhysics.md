## Summary

```csharp
public void RecreatePhysics(bool insertEntities, bool triggerNotification)
```

When physics are recreated (shape / size), this should be triggered so entities would be re-added

## Parameters

* [bool](https://docs.microsoft.com/en-us/dotnet/api/System.Boolean?view=netframework-4.6) insertEntities
* [bool](https://docs.microsoft.com/en-us/dotnet/api/System.Boolean?view=netframework-4.6) triggerNotification
