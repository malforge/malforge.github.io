## Summary

```csharp
public bool IsActiveLocal { get; }
```

Gets whether this camera is in use locally by theplayer. To determine ifplayer is controlling the camera, instead use [IsActive](Sandbox.ModAPI.Ingame.IMyCameraBlock@IsActive) .

## Returns

[bool](https://docs.microsoft.com/en-us/dotnet/api/System.Boolean?view=netframework-4.6)

