## Summary

```csharp
public long AnchorEntityId
```

Entity id of the bot anchor that spawned this bot, so the populator can re-claim it after save/load without a distance heuristic. Zero when not anchor-spawned.

## Returns

[long](https://docs.microsoft.com/en-us/dotnet/api/System.Int64?view=netframework-4.6)

