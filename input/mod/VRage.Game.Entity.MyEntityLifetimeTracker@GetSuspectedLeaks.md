## Summary

```csharp
public static void GetSuspectedLeaks(Dictionary<string, int> output)
```

Copies the current suspected-leak buckets into the supplied dictionary (cleared first). Safe to call from any thread.

## Parameters

* [Dictionary<string, int>](https://docs.microsoft.com/en-us/dotnet/api/System.Collections.Generic.Dictionary-2?view=netframework-4.6) output
