## Summary

```csharp
public Action<long> event ClaimTimerResumed
```

When claim timer resumes on some grid, grid entity id in parameter during world load

## Returns

[Action<long>](https://docs.microsoft.com/en-us/dotnet/api/System.Action-1?view=netframework-4.6)

