## Summary

```csharp
public void PinAndExecute(Action action)
```

Pins the voxel storage to prevent closing, then executes specified action. Unpins when action completes.

## Parameters

* [Action](https://docs.microsoft.com/en-us/dotnet/api/System.Action?view=netframework-4.6) action
## Summary

```csharp
public void PinAndExecute(Action<IMyStorage> action)
```

Pins the voxel storage to prevent closing, then executes specified action. Unpins when action completes.

## Parameters

* [Action<IMyStorage>](https://docs.microsoft.com/en-us/dotnet/api/System.Action-1?view=netframework-4.6) action
