## Summary

```csharp
public void GetNeighbours(ICollection<IMySlimBlock> collection)
```

The blocks that neighbour this block. Doesn't allocate memory

## Parameters

* [ICollection<IMySlimBlock>](https://docs.microsoft.com/en-us/dotnet/api/System.Collections.Generic.ICollection-1?view=netframework-4.6) collection
