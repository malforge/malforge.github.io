## Summary

```csharp
public Action<MyCubeGrid, GridLinkTypeEnum> event OnConnectionChanged
```

Event triggered, when one of GridLinkTypeEnum connections changed. Used to determine changes in connections. Use may also need react to OnGridMerge/OnGridSplit

## Returns

[Action<MyCubeGrid, GridLinkTypeEnum>](https://docs.microsoft.com/en-us/dotnet/api/System.Action-2?view=netframework-4.6)

