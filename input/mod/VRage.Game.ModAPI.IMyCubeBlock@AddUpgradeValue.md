## Summary

```csharp
public void AddUpgradeValue(string upgrade, float defaultValue)
```

Preferred way of registering a block for upgrades Adding directly to the dictionary can have unintended consequences when multiple mods are involved.

## Parameters

* [string](https://docs.microsoft.com/en-us/dotnet/api/System.String?view=netframework-4.6) upgrade
* [float](https://docs.microsoft.com/en-us/dotnet/api/System.Single?view=netframework-4.6) defaultValue
