## Summary

```csharp
public void RefreshDetailedInfo()
```

Rebuilds the detailed info from the current monitored block states. Called on the local machine after the selected-block list changes, so the display is updated without waiting for a separate server broadcast.

