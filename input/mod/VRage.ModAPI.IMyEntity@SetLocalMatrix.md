## Summary

```csharp
public void SetLocalMatrix(Matrix localMatrix, object source)
```

Sets local matrix. When entity, has parent, it's world coordinates are calculated from localMatrix and parent world matrix

## Parameters

* [Matrix](VRageMath.Matrix) localMatrix
* [object](https://docs.microsoft.com/en-us/dotnet/api/System.Object?view=netframework-4.6) source
