## Summary

```csharp
public void EnsurePhysicsSpace(BoundingBoxD aabb)
```

Ensure aabb is inside only one subspace. If no, reorder. Must not be called from parallel thread!!!

## Parameters

* [BoundingBoxD](VRageMath.BoundingBoxD) aabb
